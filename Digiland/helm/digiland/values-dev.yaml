replicaCount: 1
autoscaling:
  enabled: false

config:
  debug: "true"
  allowedHosts: "localhost,127.0.0.1"

resources:
  requests:
    cpu: 100m
    memory: 256Mi
  limits:
    cpu: 500m
    memory: 512Mi

celery:
  worker:
    replicaCount: 1
    concurrency: 2
