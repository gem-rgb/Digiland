"""Tenant-related Django signals."""
from django.db.models.signals import post_save
from django.dispatch import receiver


# Signals placeholder — tenant post-save hooks can be registered here.
# Example: auto-create default organization for new users.
